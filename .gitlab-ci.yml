stages:
  - test

test:
  stage: test
  image: docker:24.0.9
  services:
    - docker:24.0.9-dind
  script:
    - apk add --no-cache docker-compose
    - cd App
    - ./ci-app-run.sh
    - ./ci-test.sh
